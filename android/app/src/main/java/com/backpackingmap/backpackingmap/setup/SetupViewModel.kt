package com.backpackingmap.backpackingmap.setup

import androidx.lifecycle.MutableLiveData
import androidx.lifecycle.ViewModel

class SetupViewModel: ViewModel() {
    val registerSelected = MutableLiveData(true)
}